# Discord Audit Stream Configuration
# Copy this file to .env or set these environment variables

# Discord Webhook URL for sending audit messages
DISCORD_AUDIT_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Fallback channel ID if webhook fails (uses openclaw message send)
DISCORD_AUDIT_CHANNEL_ID=YOUR_CHANNEL_ID

# Send method: "webhook", "fallback", or "auto" (default)
# DISCORD_AUDIT_SEND_METHOD=auto

# Rate limiting between messages (milliseconds)
# DISCORD_AUDIT_RATE_LIMIT_MS=2000

# Batch window for grouping events (milliseconds)
# DISCORD_AUDIT_BATCH_WINDOW_MS=8000

# OpenClaw binary path
# OPENCLAW_BIN=openclaw
